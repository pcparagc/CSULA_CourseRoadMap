import React from "react";
import { BottomNavigation } from "@mui/material";
const Footer = () => {
  return (
    <BottomNavigation style={{ backgroundColor: "#000" }}>
      Index
    </BottomNavigation>
  );
};

export default Footer;
