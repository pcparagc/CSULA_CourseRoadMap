import React from "react";
import { AppBar } from "@mui/material";
import "./styles.css";
const Header = () => {
  return (
    <AppBar className="header-container" component="nav">
      {" "}
      Cal State LA
    </AppBar>
  );
};

export default Header;
